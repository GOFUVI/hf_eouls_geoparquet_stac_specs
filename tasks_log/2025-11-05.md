# Task log – 2025-11-05

## Crear la plantilla base del inventario (`TODO.md:2`)
- Elaborated a public-facing inventory template in `dataset_inventory.md`, capturing workflow names, DOI references, STAC catalog roots, and partition layouts for all external deliverables.
- Ensured the document does not reference non-public internal files and clarified that Zenodo deliveries bundle STAC/GeoParquet assets as compressed archives.
- Reformatted the content into per-workflow bullet blocks so future tasks can extend the “GeoParquet deliverables / Critical columns / STAC extensions” placeholders without reworking the structure.

## Inventario HF-radar (`TODO.md:3`)
- Rellené las secciones pendientes del inventario para `hf_radial_metrics_aws_ingestion` y `hf_eolus_geo_tools`, describiendo los GeoParquet publicados, columnas clave para filtros/particiones y las extensiones STAC realmente usadas (Table + `sci:*`/`hf_site:*` namespaces).
- Recopilé los detalles directamente de los catálogos STAC y de los scripts de ingestión/agrupación para asegurar que las descripciones reflejan los assets reales (radial metrics, header, rng_info, agregados por `node_id`, etc.).
- Dejé documentada la necesidad de revisar en futuras iteraciones si los JSON declaran explícitamente la Scientific Extension, alineando el inventario con los planes de actualización del resto de documentación.

## Inventario SAR y boyas (`TODO.md:5`)
- Extraí de los repositorios `hf_eolus_sar_ingestion` y `hf_eolus_pde_buoy_ingestion` la información sobre entregables GeoParquet (estructura de directorios, ficheros auxiliares como `lineage.json` y `ctas.sql`, y metadatos CRS) para volcarla en `dataset_inventory.md`.
- Documenté las columnas imprescindibles de cada flujo (tiempos OWI, `rowid`, máscaras ESA y componentes de viento en SAR; `timestamp`, `wind_speed`, `wind_dir`, `geometry` en boyas) destacando su papel en particiones, filtros y llaves cruzadas.
- Resumí las extensiones STAC utilizadas: Table + Processing para los Items SAR con `processing:lineage`, y Table + campos `sci:*` para la colección de boyas, señalando cómo se inyectan las propiedades desde los JSON auxiliares.
